import axios from 'axios';

export const baseUrl = 'http://127.0.0.1:1234';

export const apiEndpoint = baseUrl.concat('/api');

export const resource = axios.create({
    baseURL: apiEndpoint,
    timeout: 10000
});