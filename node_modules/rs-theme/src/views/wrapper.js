import React from 'react';
import {MessageDefault} from './message';

export const RSMuiWrapper = React.createClass({
    render(){
        return (
            <div>
                {React.cloneElement(this.props.children)}
                <MessageDefault />
            </div>
        )
    }
});